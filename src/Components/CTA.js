import { Button } from "react-bootstrap";

function CTA() {
    return (
        <Button
            className="cta-button"
            variant="success">
            Learn more</Button>
    );
};

export default CTA;