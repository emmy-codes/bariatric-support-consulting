import { Button } from "react-bootstrap";

function Order() {
    return (
        <div className="order-button">
            <Button>Here's the order button</Button>
        </div>
    );
};

export default Order;